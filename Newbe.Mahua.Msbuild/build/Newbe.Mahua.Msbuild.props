<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- try to import project-specific settings; also use fixed name that was injected into project by NuGet -->
  <PropertyGroup>
    <!-- at this point only MSBuild* property are available -->
    <MSBuildNewbeMahua>$(MSBuildProjectDirectory)\Newbe.Mahua.props</MSBuildNewbeMahua>
  </PropertyGroup>
  <Import Project="$(MSBuildNewbeMahua)" Condition="Exists($(MSBuildNewbeMahua))"/>
</Project>